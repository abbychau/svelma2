language: node_js
node_js: lts/*

branches:
  only:
    - master
    - develop

install:
  - npm i -f

cache:
  directories:
  - "./node_modules"
  - "$HOME/.npm"

jobs:
  include:
    - stage: release
      script: bash docs.sh
